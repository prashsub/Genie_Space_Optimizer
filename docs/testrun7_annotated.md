# Genie Space Optimizer — Lever Loop Test Run 7 (Annotated)

> **Run timestamp:** 2026-03-08 00:38 – 01:10 UTC (~32 minutes)
>
> **Domain:** `new_travel_booking_and_payment_analytics` (a travel booking & payment data warehouse)
>
> **Benchmark:** 20 natural-language questions with ground-truth SQL answers

---

## How to Read This Document

This document traces a single end-to-end run of the **Lever Loop** — an automated optimization pipeline that iteratively improves a Databricks Genie Space's ability to answer natural-language questions with correct SQL. The logs don't explain the hidden logic connecting each phase, so this document fills in those gaps.

### The Big Picture (TL;DR)

```
┌─────────────────────────────────────────────────────────────────────┐
│  1. PREFLIGHT — snapshot config, collect UC metadata, load         │
│     benchmarks, set up MLflow experiment                           │
│  2. BASELINE — evaluate all 20 questions (no patches yet)          │
│  3. LEVER LOOP (iterative optimization):                           │
│     For each iteration:                                            │
│       a. Analyze failures → extract root causes (ASI)              │
│       b. Cluster failures by root cause                            │
│       c. Strategist proposes fixes (patches) targeting "levers"    │
│       d. Apply patches to Genie Space (table descriptions,         │
│          instructions, example SQLs)                               │
│       e. Wait for propagation (30s)                                │
│       f. Re-evaluate all 20 questions                              │
│       g. Accept or rollback based on accuracy delta                │
│     Repeat until accuracy target met or max iterations reached     │
│  4. FINALIZE — repeatability checks, labeling session, report      │
└─────────────────────────────────────────────────────────────────────┘
```

### Key Concepts

| Term | Meaning |
|------|---------|
| **Genie Space** | A Databricks AI-powered SQL assistant configured with tables, columns, instructions, and examples to answer natural-language questions |
| **Lever** | A category of configuration change that can improve Genie's accuracy. Levers 1-5 correspond to: (1) Table & Column Descriptions, (2) Metric Views, (3) Functions, (4) Join Specifications, (5) Genie Space Instructions & Example SQLs |
| **Benchmark** | A set of question/ground-truth-SQL pairs used to measure accuracy |
| **ASI** | Automated Signal Intelligence — the system that extracts structured failure metadata (root cause, blame set, counterfactual fix) from each failed judge verdict |
| **Judge** | An automated evaluator that scores Genie's SQL on a specific dimension (syntax, schema, logic, completeness, etc.) |
| **Arbiter** | A meta-judge that makes the final call: `both_correct`, `genie_correct`, `ground_truth_correct`, or `neither_correct` |
| **Cluster** | A group of failures sharing the same root cause and blame set — used to address multiple failures with a single patch |
| **Action Group (AG)** | A bundle of proposed patches generated by the Adaptive Strategist to address one or more failure clusters |
| **Patch** | A concrete change applied to the Genie Space config (e.g., updating a table description, adding an instruction, adding an example SQL) |
| **Soft Signal** | A question that the arbiter marked as correct, but where one or more individual judges still flagged issues — indicates suboptimal quality even though the answer is technically right |
| **TVF** | Table-Valued Function — a parameterized SQL function that Genie can call. TVFs can cause routing problems when Genie picks them over a better-suited metric view or table |

---

## Phase 1: Preflight (Task-1)

```
[2026-03-08 00:38:33] [TASK-1 PREFLIGHT] Parameters
{
  "run_id": "80d15a92-75a4-4ec3-9632-2fbc0cf55c8f",
  "space_id": "01f1163c0f241707b74e83efda95edac",
  "domain": "new_travel_booking_and_payment_analytics",
  "max_iterations": 5,
  "levers": [1, 2, 3, 4, 5],
  "apply_mode": "genie_config"
}
```

> **What's happening:** The pipeline is launched with up to 5 optimization iterations allowed and all 5 levers enabled. The `apply_mode` of `genie_config` means patches will be written directly to the live Genie Space configuration via the API.
>
> **Compared to testrun2:** This run uses the same Genie Space and domain, but the pipeline architecture now has distinct task phases (PREFLIGHT → BASELINE → LEVER_LOOP → FINALIZE) instead of one monolithic run.

---

### Genie Space Inventory

```
Space ID:         01f1163c0f241707b74e83efda95edac
Tables:           8
Metric Views:     2
Functions (TVF):  0
Instructions:     1
Configured cols:  118
```

> **What's happening:** The preflight phase snapshots the current state of the Genie Space. Key observations:
>
> - **8 tables + 2 metric views** — the same 10 data sources as testrun2
> - **0 TVFs in the space** — the TVFs (`get_payment_analysis`, `get_amenity_impact`, `get_booking_trends`) still exist in Unity Catalog (15 routines discovered), but they are no longer registered as data sources in the Genie Space. Despite this, Genie can still discover and call them, which causes routing problems later
> - **118 configured columns** across all tables
> - **1 instruction** already present in the space (from prior optimization runs)

### UC Metadata Collection

```
UC Columns:     118  (covering 10 tables, source: rest_api)
Tags:             0  (source: spark)
Routines:        15  (source: rest_api)
FK Constraints:  10  (source: rest_api)
Data Profiling:  8 tables, 96 columns, 18 low-cardinality with value hints
```

> **What's happening:** The system collects rich metadata from Unity Catalog (UC) via REST API:
>
> - **118 columns** with type information, descriptions, etc.
> - **15 routines** (TVFs/UDFs) discovered in the schema — even though they're not in the Genie Space, the optimizer needs to know they exist because Genie can route to them
> - **10 foreign key constraints** — used to understand table relationships
> - **18 low-cardinality columns** identified during data profiling — candidates for value dictionaries

### Benchmark Loading

```
Target count: 20
Curated from Genie Space: 10 (10 with SQL, 0 question-only)
Existing in UC table: 22
Valid after re-validation: 22 (0 rejected)
Benchmark REJECTED on reuse (alignment):
  "List all verified and active hosts with a rating above 4.0."
    -- EXTRA_FILTER: SQL filters on is_current = true, but the question does not mention current/non-current hosts.
  "Which hosts have properties in more than one destination?"
    -- EXTRA_FILTER: SQL adds WHERE is_current = true filter not mentioned in the question.
Alignment check: 2 rejected (20 remain)
Missing curated: 1
Decision: RE-GENERATE (missing 1 curated questions)
```

> **What's happening:** The system builds a benchmark set of 20 questions. The process involves:
>
> 1. **10 curated questions** from the Genie Space's sample questions (each with SQL)
> 2. **22 existing benchmarks** from the UC benchmark table (from prior runs)
> 3. **Alignment check** — an automated quality gate that rejects benchmarks where the ground-truth SQL doesn't faithfully match the question. 2 benchmarks were rejected because their SQL added `WHERE is_current = true` filters that the natural-language question didn't mention. This is a new safety mechanism that prevents the optimizer from chasing inaccurate ground truth.
> 4. **20 benchmarks** survive after filtering
>
> **Compared to testrun2:** testrun2 used 24 benchmarks; this run uses 20. The alignment check is new — it prevents benchmark-ground-truth mismatches from contaminating the optimization.

### Experiment & Model Setup

```
Experiment:       /Shared/genie-optimization/new_travel_booking_and_payment_analytics
Model version:    m-374be6ea97b340e889c6e713e48a11d8
Judge prompts:    26 registered
Eval dataset:     synced (20 benchmarks)
```

> **What's happening:** MLflow experiment tracking is configured. A model version is registered to track all evaluation runs. 26 judge prompts are loaded — more granular than the 8 judge dimensions, because some dimensions have multiple prompt variants.

---

## Phase 2: Baseline Evaluation (Task-2)

```
[2026-03-08 00:42:22] [TASK-2 BASELINE] Benchmark dataset
{
  "benchmark_count": 20
}
```

> **What's happening:** The baseline phase evaluates all 20 questions against Genie *before* any optimization patches are applied. This establishes the starting accuracy that the lever loop needs to improve.

### Baseline Question Results

**11 questions passed all judges. 9 had at least one failure.**

Here are the failures and their diagnoses:

#### Q1: "Show the monthly payment trend by payment method for the year 2025."

```
Genie SQL: SELECT * FROM get_payment_analysis(
  start_date => '2025-01-01', end_date => '2025-12-31',
  payment_method_filter => 'NULL')
Result: 0 rows returned (GT: 35 rows)
Arbiter: ground_truth_correct
```

> **Diagnosis:** Genie routed to the TVF `get_payment_analysis` instead of the `payment_analytics_metrics` metric view. Worse, it passed `'NULL'` as a string literal rather than actual `NULL`, which likely filtered out all rows, returning 0 results. The ground truth uses the metric view with `DATE_FORMAT` and `GROUP BY` to produce a proper monthly trend. This is the hardest failure — it persists through all iterations.

#### Q4: "Which amenity categories are most common across properties?"

```
Genie SQL: SELECT * FROM get_amenity_impact(
  start_date => '2025-03-08', end_date => '2026-03-08')
Result: 38 rows (GT: 4 rows)
Arbiter: ground_truth_correct
```

> **Diagnosis:** Genie routed to the TVF `get_amenity_impact` instead of doing a simple `JOIN` between `bridge_property_amenity` and `dim_amenity` with a `GROUP BY category`. The TVF returns per-amenity impact data (38 rows at individual amenity granularity) instead of per-category counts (4 rows). Classic asset routing error — the TVF name sounds relevant but returns the wrong grain.

#### Q5, Q7, Q10, Q14, Q17: Cosmetic Mismatches (both_correct)

> These 5 questions were judged `both_correct` by the arbiter despite `result_correctness` failures. The mismatches are cosmetic:
> - **Q5 & Q14:** Different column aliases (`total_bookings` vs `booking_count`) or sort order
> - **Q7:** Different columns selected (`destination_country` vs `destination_name`) but same filtering logic
> - **Q10:** GT includes `COUNT(*)` as an extra column; Genie omits it
> - **Q17:** Genie uses `SELECT *` while GT selects specific columns
>
> These are "soft signals" — technically correct answers that could be improved stylistically.

#### Q9: "Which properties have bookings where guests exceeded max_guests?"

```
Genie SQL: includes booking_id, no DISTINCT → 4216 rows
GT SQL: uses SELECT DISTINCT on property-level fields → 3746 rows
Arbiter: both_correct
```

> **Diagnosis:** Genie returns individual booking rows while the GT deduplicates to unique property-guest combinations. Both correctly answer "which properties" but at different granularity. The arbiter correctly marks this as `both_correct` since the core logic is right.

#### Q19: "Total revenue by property type for confirmed bookings, broken down by quarter of check-in date?"

```
Genie SQL: DATE_TRUNC('QUARTER', check_in_date) → 30 rows (per year-quarter)
GT SQL: QUARTER(check_in_date) → 12 rows (quarter number 1-4 across all years)
Arbiter: ground_truth_correct
```

> **Diagnosis:** Genie used `DATE_TRUNC('QUARTER', ...)` which preserves the year (producing "2024-01-01", "2024-04-01", etc.), while the GT uses `QUARTER(...)` which extracts just the quarter number (1-4) and aggregates across years. "Broken down by quarter" is ambiguous, but the GT interpretation (quarter number) is treated as the correct one.

### Baseline Score Summary

```
syntax_validity      100.0  (threshold: 98.0)  PASS
schema_accuracy       85.0  (threshold: 95.0)  FAIL <<<
logical_accuracy      85.0  (threshold: 90.0)  FAIL <<<
semantic_equivalence  85.0  (threshold: 90.0)  FAIL <<<
completeness          85.0  (threshold: 90.0)  FAIL <<<
response_quality     100.0  (threshold: 0.0)   PASS
asset_routing         95.0  (threshold: 95.0)  PASS
result_correctness    85.0  (threshold: 85.0)  PASS

Arbiter: both_correct=17 (85.0%), ground_truth_correct=3 (15.0%)
Overall accuracy: 85.0%
Thresholds met: NO
Failed questions: [_018, _012, _013]  (Q1, Q4, Q19 above)
```

> **How to read this:**
>
> - **17 of 20** questions marked `both_correct` (85.0% accuracy)
> - **3 hard failures** — all marked `ground_truth_correct` (Genie got them wrong)
> - **4 judge dimensions** are below threshold — the system needs to improve
> - **syntax_validity = 100%** — Genie always produces valid SQL
> - **asset_routing = 95%** — only 1 routing failure out of 20 (Q1)
> - **response_quality = 100%** — Genie's natural-language summaries are all good
>
> **Compared to testrun2:** testrun2 started at 87.5% with 24 benchmarks. This run starts at 85.0% with 20 benchmarks, but has fewer hard failures (3 vs 3) and zero `neither_correct` verdicts.

---

## Phase 3: Lever Loop (Task-3)

### Pre-Loop Enrichment

#### Description Enrichment

```
Eligible columns: 0 | Enriched: 0
Eligible tables: 0 | Enriched: 0
Genie API PATCH sent: NO
```

> All columns and tables already have descriptions. Nothing to do.

#### Join Discovery

```
Existing join specs:         0
FK constraint candidates:    0
Execution-proven candidates: 2
New joins applied:           2
  fact_booking <-> dim_property ON property_key (freq=2, agreed)
  bridge_property_amenity <-> dim_amenity ON amenity_id (freq=1, single_source)
Genie API PATCH sent: YES
```

> **What's happening:** Unlike testrun2 (which had 5 existing join specs), this run starts with **0 join specs** — they were likely cleared between runs. The system discovers 2 join relationships by executing test queries and applies them. These joins help Genie know how to correctly combine tables.
>
> **Hidden logic:** "freq=2, agreed" means two independent sources (FK constraints and execution testing) both confirmed the `fact_booking ↔ dim_property` join. "freq=1, single_source" means only execution testing confirmed the amenity join.

#### Proactive Instruction Seeding

```
Instructions: generated (1249 chars)
```

> **What's happening:** The system generates a baseline instruction document covering the space's purpose, asset routing guidance, and general SQL patterns. This is written to the Genie Space as a starting point for the optimizer to refine.

#### Proactive Benchmark Example SQLs

```
Mined proposals: 20
Applied: 20
  [1] Show the monthly payment trend by payment method for the year 2025.
  [2] How many verified vs unverified hosts are there in each country?
  ... (all 20 benchmark questions)
  [20] What is the distribution of dates across each day of the week...
```

> **What's happening:** All 20 benchmark questions are mined into example SQL entries in the Genie Space. These serve as few-shot patterns for Genie to follow.
>
> **Compared to testrun2:** testrun2 applied 19 of 24 examples. This run applies all 20 — the alignment check earlier ensures every benchmark has a valid, faithful SQL, so all can be used as examples.

---

## Phase 4: Failure Analysis

### Triage Results

```
Total rows loaded:           20
Arbiter verdicts:            both_correct=17, ground_truth_correct=3
Excluded (fully correct):    11 question(s)
Soft signals:                6 question(s)
Hard failure rows:           3
```

> **How to read this:**
>
> - **11 questions:** fully correct — no judge flagged any issue
> - **6 questions:** technically correct (arbiter = `both_correct`), but individual judges flagged quality issues — these are soft signals
> - **3 questions:** hard failures that need fixing (Q1, Q4, Q19)

### Soft Signal Details

```
_016 (top 5 destinations): result_correctness only
_015 (highest completion rate): result_correctness only
_019 (guests exceeded max_guests): schema_accuracy, completeness, logical_accuracy,
                                    result_correctness, semantic_equivalence
_gs_002 (monthly trend fact_payment): result_correctness only
_gs_010 (bookings by property type): logical_accuracy, result_correctness,
                                      semantic_equivalence
_gs_001 (bookings + nights by status): result_correctness only
```

> **How to read this:** Most soft signals only have `result_correctness` failures — meaning the results differ in hash but the arbiter judged both correct (cosmetic differences like column aliases or sort order). Q19 (guests exceeded capacity) has 5 judge failures, making it the most significant soft signal — though still correct overall.

---

## Phase 4a: ASI Extraction (Root Cause Analysis)

### Hard Failure — Q1 (`_018`): "Show the monthly payment trend by payment method for the year 2025."

```
Judge: schema_accuracy    | Verdict: FAIL | Root cause: wrong_table
  blame: ['get_payment_analysis']
  counterfactual: "The TVF returned 0 rows (likely due to passing NULL as string literal)"

Judge: asset_routing      | Verdict: FAIL | Root cause: asset_routing_error
  blame: ['asset_routing:MV']
  counterfactual: "Add instruction to prefer MV for this query pattern"

Judge: logical_accuracy   | Verdict: FAIL | Root cause: missing_filter
  blame: ['get_payment_analysis', 'payment_method_filter']
  counterfactual: "Clearer TVF documentation for NULL handling"

Judge: completeness       | Verdict: FAIL | Root cause: missing_filter
  blame: ['TVF returning 0 rows', 'missing monthly grouping', 'missing payment_method dimension']

Judge: semantic_equivalence | Verdict: FAIL | Root cause: missing_filter
Judge: result_correctness   | Verdict: FAIL | Root cause: missing_filter
Judge: response_quality     | Verdict: FAIL | Root cause: missing_filter

Dominant root cause: missing_filter
```

> **How to read this:** This question has **7 judge failures** — every judge except `syntax_validity` failed. The root causes split between:
> - `wrong_table` — Genie chose the TVF instead of the metric view
> - `asset_routing_error` — confirms the routing problem
> - `missing_filter` — the TVF call's `'NULL'` string literal filter caused empty results
>
> The dominant root cause is `missing_filter` by majority vote, but the underlying problem is really asset routing: Genie should use `payment_analytics_metrics` instead of `get_payment_analysis`.

### Hard Failure — Q4 (`_012`): "Which amenity categories are most common across properties?"

```
Judge: schema_accuracy    | Verdict: FAIL | Root cause: wrong_table
  blame: ['get_amenity_impact']
  counterfactual: "Route to bridge_property_amenity joined with dim_amenity"

Judge: logical_accuracy   | Verdict: FAIL | Root cause: wrong_aggregation
  blame: ['get_amenity_impact']

Judge: completeness       | Verdict: FAIL | Root cause: wrong_aggregation
  blame: ['missing_aggregation_by_category', 'extra_columns_and_filters']

Judge: result_correctness | Verdict: FAIL | Root cause: wrong_aggregation
Judge: semantic_equivalence | Verdict: FAIL | Root cause: wrong_aggregation
Judge: response_quality     | Verdict: FAIL | Root cause: wrong_join

Dominant root cause: wrong_aggregation
```

> **How to read this:** Genie used the `get_amenity_impact` TVF which returns per-amenity data, not per-category counts. The ASI correctly identifies that the question needs a simple `GROUP BY category` on `bridge_property_amenity ↔ dim_amenity`, not a TVF call.

### Hard Failure — Q19 (`_013`): "Total revenue by property type for confirmed bookings, broken down by quarter?"

```
Judge: schema_accuracy    | Verdict: FAIL | Root cause: wrong_column
  blame: ['DATE_TRUNC(QUARTER, check_in_date)']
  counterfactual: "Replace DATE_TRUNC with QUARTER() to return quarter number 1-4"

Judge: logical_accuracy   | Verdict: FAIL | Root cause: wrong_filter_condition
  blame: ['check_in_date']

Judge: completeness       | Verdict: FAIL | Root cause: wrong_filter_condition
  blame: ['QUARTER(check_in_date) vs DATE_TRUNC(QUARTER, check_in_date)']

Judge: result_correctness | Verdict: FAIL | Root cause: wrong_aggregation
Judge: semantic_equivalence | Verdict: FAIL | Root cause: wrong_filter_condition

Dominant root cause: wrong_filter_condition
```

> **How to read this:** Genie used `DATE_TRUNC('QUARTER', ...)` which returns full timestamps (`2024-01-01`, `2024-04-01`, ...) producing 30 rows across all year-quarters. The GT uses `QUARTER(...)` which returns integers (1-4), collapsing across years into 12 rows (4 quarters × 3 property types). The fix is straightforward: teach Genie to use `QUARTER()` for quarter-number breakdowns.

---

## Phase 4b: Cluster Formation

```
Total failure entries: 18 (across 3 questions, 7 judges)
Question profiles:    3
Cluster groups:       3
  C001 (missing_filter, TVF routing): Q1 — payment trend [_018]
  C002 (wrong_aggregation, TVF routing): Q4 — amenity categories [_012]
  C003 (wrong_filter_condition, DATE_TRUNC vs QUARTER): Q19 — revenue by quarter [_013]
```

> **What's happening:** The 18 individual judge failures are grouped into **3 clusters** based on shared root causes. Each cluster represents a distinct problem:
>
> - **C001:** TVF `get_payment_analysis` routing + NULL string literal
> - **C002:** TVF `get_amenity_impact` routing + wrong aggregation grain
> - **C003:** `DATE_TRUNC` vs `QUARTER()` function choice
>
> **Hidden logic:** Clusters C001 and C002 share a common theme — TVF over-routing — but are kept separate because they have different blame sets and would need different fixes.

### Soft Signal Clusters

```
Soft signal rows:    6
Soft clusters:       3
  Cluster 1 (wrong_aggregation, no blame): 4 questions [_015, _016, _gs_001, _gs_002]
  Cluster 2 (wrong_join, blame=DISTINCT/booking_id): 1 question [_019]
  Cluster 3 (wrong_aggregation, blame=property_type): 1 question [_gs_010]
```

> **What's happening:** The 6 soft signals are clustered into 3 groups. These provide additional context to the strategist but don't drive primary optimization. Most soft signal failures are cosmetic `result_correctness` mismatches (hash differs due to column aliases or sort order).

### Failure Clusters with Lever Mapping

```
Cluster 1/3: missing_filter     → Lever 1 (Tables & Columns)
Cluster 2/3: wrong_aggregation  → Lever 1 (Tables & Columns)
Cluster 3/3: wrong_filter_condition → Lever 1 (Tables & Columns)

Summary: All 3 clusters map to Lever 1
Top root causes: missing_filter (1), wrong_aggregation (1), wrong_filter_condition (1)
```

> **What's happening:** All clusters initially map to Lever 1 (table/column descriptions). However, the strategist has freedom to use any lever it deems appropriate — the mapping is a recommendation, not a constraint.

---

## Phase 4c: Pipeline Lineage

```
Q: _018 (payment trend) → Cluster C001 → Lever 1
  Counterfactual: "TVF returned 0 rows due to NULL string literal"

Q: _012 (amenity categories) → Cluster C002 → Lever 1
  Counterfactual: "Route to bridge_property_amenity ↔ dim_amenity instead of TVF"

Q: _013 (revenue by quarter) → Cluster C003 → Lever 1
  Counterfactual: "Replace DATE_TRUNC with QUARTER() for quarter numbers"
```

> **What's happening:** Full traceability from question → failure analysis → cluster → lever. This allows auditing every optimization decision.

---

## Phase 5: The Lever Loop — Iteration 1

### Adaptive Strategist

```
STRATEGIST INPUT (Iteration 1)
  Success Summary: 17 of 20 benchmarks pass. 3 failures remain.
  Clusters: 3 hard, 3 soft
  Reflections: 0
  Prompt: 77,437 chars
  Priority Ranking:
    Rank 1: [C001] missing_filter (judge=asset_routing, impact=3.5)
    Rank 2: [C002] wrong_aggregation (judge=completeness, impact=1.5)
    Rank 3: [C003] wrong_filter_condition (judge=completeness, impact=1.4)
```

> **What's happening:** The Adaptive Strategist receives a 77K-character prompt containing:
> - All 3 hard failure clusters and 3 soft signal clusters
> - The full Genie Space configuration
> - Benchmark questions and ground-truth SQLs
> - No reflections yet (first iteration)
>
> The priority ranking uses an impact score that considers the number of failed judges and the severity of each failure. C001 (payment trend) has the highest impact (3.5) because it has 7 judge failures.
>
> **Compared to testrun2:** testrun2's strategist had a 52K-char prompt. This run's 77K prompt is larger, likely due to the richer UC metadata and more detailed failure context from the new ASI extraction.

### Strategist Output

```
AG1: "Multiple questions are being incorrectly routed to TVFs
  (get_payment_analysis, get_amenity_impact) instead of using metric views
  or direct table queries, and the model adds unnecessary IS NOT NULL
  filters and uses DATE_TRUNC instead of QUARTER() for quarter breakdowns."
Levers: 1, 5
Affected Questions: 3
```

> **What's happening:** The strategist produces a unified diagnosis covering all 3 hard failures. It identifies three interrelated problems:
>
> 1. **TVF over-routing** — Genie picks TVFs over metric views/direct queries
> 2. **Unnecessary IS NOT NULL filters** — adding `WHERE property_type IS NOT NULL` etc. when not needed
> 3. **DATE_TRUNC vs QUARTER()** — using the wrong function for quarter-number extraction
>
> The strategy targets Levers 1 (table descriptions) and 5 (instructions + example SQLs).

### Proposals

```
Proposal 1/6: update_description (fact_booking) — Lever 1
  best_for: "Booking counts, revenue analysis, length of stay, guest counts.
    CRITICAL: For quarterly breakdowns, use QUARTER(check_in_date)..."

Proposal 2/6: rewrite_instruction — Lever 5
  "PURPOSE: Analytics for Wanderbricks vacation rental platform...
   ASSET ROUTING: Monthly payment trends → payment_analytics_metrics MV..."
  (4,118 chars)

Proposal 3/6: add_example_sql — Lever 5
  Q: "Show the monthly payment trend by payment method for the year 2025."
  SQL: SELECT DATE_FORMAT(payment_date, 'yyyy-MM') AS payment_month,
       payment_method, MEASURE(total_payment_amount)...

Proposal 4/6: add_example_sql — Lever 5
  Q: "Which amenity categories are most common across properties?"
  SQL: SELECT da.category, COUNT(*) AS total_associations
       FROM bridge_property_amenity bpa
       JOIN dim_amenity da ON bpa.amenity_id = da.amenity_id
       GROUP BY da.category ORDER BY total_associations DESC

Proposal 5/6: add_example_sql — Lever 5
  Q: "What is the total revenue by property type for confirmed bookings,
      broken down by quarter of check-in date?"
  SQL: SELECT property_type, QUARTER(check_in_date) AS quarter,
       MEASURE(total_revenue)... GROUP BY property_type, QUARTER(check_in_date)

Proposal 6/6: add_example_sql — Lever 5
  Q: "What is the total number of bookings by property type?"
  SQL: SELECT property_type, MEASURE(booking_count) AS total_bookings...
       GROUP BY property_type ORDER BY total_bookings DESC
```

> **What's happening:** The strategist generated 6 proposals:
>
> - **Lever 1:** Update `fact_booking` table description to include guidance on using `QUARTER()` for quarter breakdowns
> - **Lever 5:** Rewrite the instruction document with explicit asset routing rules (payment queries → MV, amenity queries → JOIN tables)
> - **Lever 5:** 4 targeted example SQLs — one for each failing question plus one bonus (bookings by property type) that addresses a soft signal
>
> **Rejected proposal:** 1 example SQL was rejected because it returned 0 rows:
> ```
> Rejecting add_example_sql: EMPTY_RESULT: Query returned 0 rows
> ```
> This was for the payment trend query using `MEASURE()` on the metric view — likely a filter/date issue. The system validates every SQL by executing it before applying.
>
> **Compared to testrun2:** testrun2's first iteration had 6 proposals across Levers 1, 2, and 5. This run's 6 proposals target only Levers 1 and 5 — no Lever 2 (Metric View column descriptions) because the strategist determined that table descriptions and instructions are sufficient.

### Patches Applied

```
[1] Update Table Description: fact_booking
[2] Rewrite Instruction (4,118 chars)
[3] Add Example SQL: "Show the monthly payment trend..."
[4] Add Example SQL: "Which amenity categories are most common..."
[5] Add Example SQL: "Total revenue by property type by quarter..."
[6] Add Example SQL: "Total number of bookings by property type?"
```

> **What's happening:** 6 patches are applied to the live Genie Space via the Databricks API. The instruction rewrite replaces the existing instruction entirely (not a merge), while example SQLs are additive.

### Propagation Wait

```
AG1: 6 patches applied
Patched objects: C001, fact_booking
Wait time: 30s
```

> **What's happening:** The system waits 30 seconds for the Genie Space to re-index the updated metadata.

---

### Evaluation — Iteration 1

```
20 questions: 17 all-pass, 3 with failures
```

**Key results:**

#### Q4 (amenity categories): NOW CORRECT

> Genie now uses the direct `JOIN` query: `bridge_property_amenity ↔ dim_amenity` with `GROUP BY category`. The example SQL patch directly guided this change. Previously routed to `get_amenity_impact` TVF.

#### Q14 (bookings by property type): NOW ALL-PASS

> Previously a soft signal (cosmetic hash mismatch). The example SQL patch guided Genie to produce nearly identical SQL to the ground truth. The `IS NOT NULL` filter was dropped.

#### Q18 (top 10 destinations): Minor regression

> Now has a `response_quality` failure — Genie's natural-language analysis truncates before listing all 10 destinations. This is cosmetic and doesn't affect the SQL correctness. Arbiter still says `both_correct`.

#### Q19 (revenue by quarter): NOW CORRECT

> Genie now uses `QUARTER(check_in_date)` instead of `DATE_TRUNC('QUARTER', ...)`, producing the correct 12-row result grouped by quarter number (1-4).

#### Q1 (payment trend): STILL FAILING

> Genie still routes to `get_payment_analysis` TVF with `payment_method_filter => 'NULL'`. Despite the instruction explicitly saying to use `payment_analytics_metrics` and the example SQL showing the correct pattern, Genie's routing preference for the TVF is too strong.

### Score Summary — Iteration 1

```
syntax_validity      100.0  (threshold: 98.0)  PASS
schema_accuracy       95.0  (threshold: 95.0)  PASS
logical_accuracy      95.0  (threshold: 90.0)  PASS
semantic_equivalence  95.0  (threshold: 90.0)  PASS
completeness          95.0  (threshold: 90.0)  PASS
response_quality      94.7  (threshold: 0.0)   PASS
asset_routing         95.0  (threshold: 95.0)  PASS
result_correctness    95.0  (threshold: 85.0)  PASS

Arbiter: both_correct=19 (95.0%), ground_truth_correct=1 (5.0%)
Overall accuracy: 95.0%
Thresholds met: YES ✓
Failed questions: [_018 only]
```

> **This is a remarkable result.** In a single iteration, accuracy jumped from **85.0% → 95.0% (+10.0%)** and **all thresholds are now met**. Only 1 question remains failed (Q1 — payment trend TVF routing).
>
> **Confirmation eval: SKIPPED** — because accuracy improved so significantly (85% → 95%), the system skips the confirmation evaluation. The improvement is large enough that variance won't matter.

```
FULL EVAL [AG1]: PASS — ACCEPTED
Accuracy: 85.0% → 95.0% (+10.0%)
```

> **Compared to testrun2:** testrun2 needed 5 iterations and peaked at 95.8% before settling at 93.75%. This run converges in a single iteration at 95.0% — faster and with a better final result. Key reasons:
>
> 1. **Benchmark alignment check** removes noisy/unfaithful benchmarks upfront
> 2. **Richer ASI extraction** gives the strategist better root cause information
> 3. **All 20 example SQLs** are applied proactively before the loop starts
> 4. **No TVFs in the Genie Space** (vs testrun2 which may have had TVFs registered) — though Genie can still discover them in UC

---

## Phase 6: Finalize (Task-4)

### Repeatability Evaluation

```
REPEATABILITY EVALUATION: final_1
  Repeatability (headline):    100.0%
  Execution equivalence:       100.0%
  Structural equivalence:      100.0%
  Exact SQL match:             100.0%
  result_correctness:          85.0
  repeatability:               50.0
  asset_routing:               95.0

REPEATABILITY EVALUATION: final_2
  Repeatability (headline):    100.0%
  Execution equivalence:       100.0%
  Structural equivalence:      100.0%
  Exact SQL match:             100.0%
  result_correctness:          90.0
  repeatability:               55.0
  asset_routing:               95.0
```

> **What's happening:** The finalize phase runs 2 additional full evaluations to measure **repeatability** — whether Genie produces the same SQL when asked the same questions multiple times.
>
> - **Headline repeatability: 100%** — Genie produced identical SQL in both runs. This is excellent consistency.
> - **Exact SQL match: 100%** — not just equivalent, but character-for-character identical
> - **result_correctness varies (85% / 90%)** — this is because result correctness depends on hash comparison, and some cosmetic differences (column aliases, sort order) cause hash mismatches even when the SQL is identical
> - **repeatability score (50% / 55%)** — this is a stricter metric that factors in result hash matching, not just SQL matching
>
> **Why repeatability matters:** If Genie's SQL varies across runs, optimization is unreliable — a patch might appear to help but the improvement was just lucky variance. 100% SQL repeatability confirms the patches had a genuine, stable effect.

### Human Review Labeling Session

```
[Labeling] Creating session 'opt_new_travel_booking_and_payment_anal_80d15a92_20260308_010904'
  Traces: 40
  URL: https://fe-sandbox-test-genie-accelerator.cloud.databricks.com/ml/review-v2/...
```

> **What's happening:** The system creates an MLflow labeling session with 40 traces (20 questions × 2 evaluation runs) for human review. This allows domain experts to review the judge verdicts, correct any mislabeled results, and provide feedback for future optimization runs.

### Convergence

```
Status: CONVERGED
Convergence reason: threshold_met
Repeatability: 100.0%
Elapsed: 577.4 seconds (~9.6 minutes for finalize phase)
```

> **What's happening:** The run is complete. The system converged because all judge thresholds were met after iteration 1. No further iterations were needed.

---

## Final Summary

```
OPTIMIZATION RUN SUMMARY
  Baseline accuracy:       85.0%
  Final accuracy:          95.0%
  Net improvement:         +10.0%
  Iterations:              1
  Best iteration:          1

  Proactive Changes (pre-lever-loop):
    Join specs discovered:       2
    Instructions seeded:         1249 chars
    Benchmark examples mined:    20

  Lever Loop Changes:
    Action groups attempted:     2
    Action groups accepted:      1
    Action groups rolled back:   0

  AG1: TVF routing + DATE_TRUNC/QUARTER + unnecessary filters
    Accuracy delta: +10.0%
    Patches: 1 table description, 1 instruction rewrite, 4 example SQLs

  Final Scores:
    asset_routing:           95.0
    completeness:            95.0
    logical_accuracy:        95.0
    repeatability:           0.0
    response_quality:        94.7
    result_correctness:      95.0
    schema_accuracy:         95.0
    semantic_equivalence:    95.0
    syntax_validity:         100.0
```

> **How to read this:**
>
> - **Final accuracy: 95.0%** — up from 85.0% baseline
> - **1 iteration** was sufficient (out of 5 allowed)
> - **Levers 1 and 5** were used (Table Descriptions + Instructions/Examples)
> - **Levers 2, 3, and 4** were never needed (Metric Views, Functions, Joins)
> - **2 action groups** were generated (the system counts the initial proactive enrichments as AG0), but only AG1 was the optimization loop's work
> - **0 rollbacks** — everything that was applied was kept
>
> **Accuracy progression:**
>
> | Phase | Accuracy | Delta | Key Change |
> |-------|----------|-------|------------|
> | Baseline | 85.0% | — | Starting point (3 hard failures) |
> | Iteration 1 | 95.0% | +10.0% | TVF routing instructions + QUARTER() example + amenity JOIN example |
>
> **What remained unfixed:** Q1 ("Show the monthly payment trend by payment method for the year 2025") — where Genie insists on using the `get_payment_analysis` TVF instead of the `payment_analytics_metrics` metric view. This is likely because:
>
> 1. The TVF name `get_payment_analysis` is a strong semantic match for "payment trend"
> 2. The TVF exists in Unity Catalog even though it's not registered in the Genie Space — Genie can still discover and call it
> 3. The TVF's `payment_method_filter => 'NULL'` string literal issue causes 0 rows
> 4. Instructions and examples weren't strong enough to override the routing preference
>
> Fixing this might require: removing the TVF from Unity Catalog, renaming it, or adding more aggressive routing constraints. This is a known limitation of the lever loop — it can only modify Genie Space configuration, not the underlying UC schema.

---

## Appendix: Comparison with Test Run 2

| Dimension | Test Run 2 | Test Run 7 |
|-----------|-----------|-----------|
| **Benchmarks** | 24 | 20 (2 rejected by alignment check) |
| **Baseline accuracy** | 87.5% | 85.0% |
| **Final accuracy** | 93.75% | 95.0% |
| **Iterations used** | 5 | 1 |
| **Total runtime** | ~71 minutes | ~32 minutes |
| **TVFs in space** | Present | Removed (but still in UC) |
| **Rollbacks** | 2 | 0 |
| **Persistent failure** | Monthly booking trends (TVF routing) | Monthly payment trend (TVF routing) |
| **Pipeline architecture** | Monolithic | 4-task: Preflight → Baseline → Lever Loop → Finalize |
| **Repeatability check** | Not shown | 100% (2 runs) |
| **Alignment check** | Not present | Present (2 benchmarks rejected) |
| **Human review** | Not shown | Labeling session with 40 traces |

> **Key takeaway:** Test Run 7 converges dramatically faster (1 iteration vs 5) and achieves a slightly better accuracy (95.0% vs 93.75%), likely due to:
>
> 1. The benchmark alignment check removing noisy ground-truth pairs
> 2. Richer ASI failure context enabling better strategist proposals
> 3. All example SQLs being applied proactively before the loop starts
> 4. A more mature Genie Space starting point (prior optimizations already applied descriptions, etc.)
> 5. The 4-task architecture providing cleaner phase separation

---

## Appendix: Judge Evaluation Format

Each question evaluation follows this template:

```
--- Q[N]: q[N] --------------------------------------------------------
| Question:  "[natural language question]"
|
| Genie SQL:
|   [SQL generated by Genie]
| Genie Status: MessageStatus.COMPLETED
| Genie Analysis: [Genie's natural language summary of results]
|
| Ground Truth SQL:
|   [Expected SQL from benchmark]
|
| Result Comparison: Match: YES/NO (method) | GT rows: N | Genie rows: N
|   GT hash: XXXX | Genie hash: XXXX
|   [Sample rows from both result sets]
|
| Judge Verdicts:
|   syntax_validity          PASS/FAIL  -- [rationale]
|   schema_accuracy          PASS/FAIL  -- [rationale]
|   logical_accuracy         PASS/FAIL  -- [rationale]
|   semantic_equivalence     PASS/FAIL  -- [rationale]
|   completeness             PASS/FAIL  -- [rationale]
|   response_quality         PASS/FAIL  -- [rationale]
|   asset_routing            PASS/FAIL  -- [rationale]
|   result_correctness       PASS/FAIL  -- [rationale]
|   arbiter                  [verdict]  -- [rationale]
```

### Judge Dimensions Explained

| Judge | What It Checks |
|-------|---------------|
| **syntax_validity** | Is the SQL syntactically correct? Can it parse and execute? |
| **schema_accuracy** | Does the SQL reference the correct tables and columns? |
| **logical_accuracy** | Does the SQL logic match what the question is asking? |
| **semantic_equivalence** | Are the Genie and ground-truth SQLs semantically equivalent (same meaning, even if different syntax)? |
| **completeness** | Does the SQL capture all aspects of the question (filters, groupings, etc.)? |
| **response_quality** | Is Genie's natural-language analysis of the results accurate and helpful? |
| **asset_routing** | Did Genie choose the right table/view/function to query? (e.g., metric view vs base table vs TVF) |
| **result_correctness** | Do the actual query results match between Genie and ground truth? (Row counts, hash comparison) |
| **arbiter** | Final verdict combining all judges: `both_correct`, `genie_correct`, `ground_truth_correct`, or `neither_correct` |

### Result Comparison Methods

| Method | Meaning |
|--------|---------|
| `identical_sql` | Genie produced exactly the same SQL as ground truth |
| `exact` | Different SQL, but results are identical (same rows, same hash) |
| `hash` | Results match by hash comparison (faster than row-by-row) |
| `signature` | Results match structurally (same shape, compatible values) |
| `mismatch` | Results differ — different row counts or different hashes |
